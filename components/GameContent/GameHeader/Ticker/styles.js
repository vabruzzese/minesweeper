import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  ticker: {
    borderWidth: 2,
    borderColor: '#000',
    height: 35,
    width: 35,
    justifyContent: 'center',
    alignItems: 'center',
    margin: 5,
  },
  text: {
    fontWeight: 'bold',
  },
});
